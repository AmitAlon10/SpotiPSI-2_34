import { createTheme} from '@mui/material/styles';

export const appTheme = createTheme({
  palette: {
    primary: {
      main: '#aeafae',
    },
    secondary: {
      main: '#c370fac9',
      light: '#F5EBFF',
      contrastText: '#47008F',
    }
  }
});
